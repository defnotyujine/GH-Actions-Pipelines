name: CI

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Placeholder check
        run: |
          echo "CI trigger and status reporting are working."
          echo "This step will be replaced by Molecule/Goss validation of the playbook."
